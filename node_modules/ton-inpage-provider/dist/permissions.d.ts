import { WalletContractType } from './models';
export interface Permissions {
    tonClient: true;
    accountInteraction: {
        address: string;
        publicKey: string;
        contractType: WalletContractType;
    };
}
export declare type Permission = keyof Permissions;
export declare type PermissionData<T extends Permission> = Permissions[T];
